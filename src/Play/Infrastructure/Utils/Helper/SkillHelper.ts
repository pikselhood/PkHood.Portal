import * as SkillObj from "../../../Object/Skill/SkillObject";
import * as THREE from "three";
import { AnimationType } from "../../Animation/AnimationType";

export default class SkillHelper{

    // static GetSkill(skillId: number, origin: THREE.Object3D, target: THREE.Object3D): SkillObj.SkillObject {
    //     switch (skillId) {
    //         case 1:
    //             return new SkillObj.ArcherBasicAttackObject(origin, target);
    //         default:
    //             return new SkillObj.ArcherBasicAttackObject(origin, target);
    //     }
    // }

    // static GetAnimationType(skillId: number): AnimationType {
    //     switch (skillId) {
    //         case 1:
    //         case 2:
    //         case 3:
    //         case 4:
    //             return AnimationType.BasicAttack
    //         default:
    //             return AnimationType.BasicAttack
    //     }
    // }

}